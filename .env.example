# Database Connection
# Replace with your actual PostgreSQL connection string
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/lab_orders"

# NextAuth Configuration
# Generate a secure random string for production
# You can generate one using: openssl rand -base64 32
# Or in PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes([System.Guid]::NewGuid().ToString() + [System.Guid]::NewGuid().ToString()))
NEXTAUTH_SECRET="your-secret-key-here-generate-a-random-string"

# Application URL
# For development, use http://localhost:PORT
# For production, use your actual domain
NEXTAUTH_URL="http://localhost:3000"

# Environment
# Options: development, production, test
NODE_ENV="development"
